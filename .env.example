# SHARED INFRASTRUCTURE CONFIGURATION
# This file contains environment variables for shared services (nginx, cloudflare tunnel)
#
# Copy this to .env in root directory:
#   cp .env.shared .env

# Cloudflare Tunnel Token
# Get this from: https://dash.cloudflare.com/ → Zero Trust → Tunnels
CLOUDFLARED_TOKEN=

# Shared Gateway Configuration
GATEWAY_PORT=8080
GATEWAY_HOST=localhost
